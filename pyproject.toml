[project]
name = "romicore-sdk-asyncapi"
version = "0.1.0"
description = "AsyncAPI (MQTT) specification and documentation site for the RomiCore SDK."
readme = "README.md"
license = "MIT"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "zensical>=0.0.33",
]
